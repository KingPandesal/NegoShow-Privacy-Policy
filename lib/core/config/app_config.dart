/// Central place for runtime configuration.
///
/// This template intentionally does NOT include any Firebase project-specific
/// configuration files (like `firebase_options.dart`, `google-services.json`,
/// or `GoogleService-Info.plist`).
///
/// After creating your Firebase project and registering your app, run:
///
///   flutterfire configure
///
/// This will add the required per-platform Firebase configuration files to
/// your app (which will be ignored by git via `.gitignore`).
class AppConfig {
  const AppConfig();
}

